server.port=9090
spring.application.name=admin-dashboard
# disable security for demo
spring.security.enabled=false

# don't error when no Thymeleaf templates are present (demo app)
spring.thymeleaf.check-template-location=false
